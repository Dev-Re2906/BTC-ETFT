{
  "name": "btcetf-token-deployment",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network bsc",
    "test": "hardhat test",
    "build": "hardhat compile",
    "verify": "hardhat verify --network bsc"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "hardhat": "^2.24.3"
  }
}